import { IngredientsValidatorDirective } from './ingredients-validator.directive';

describe('IngredientsValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new IngredientsValidatorDirective();
    expect(directive).toBeTruthy();
  });
});

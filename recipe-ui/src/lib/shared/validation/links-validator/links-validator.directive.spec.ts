import { LinksValidatorDirective } from './links-validator.directive';

describe('LinksValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new LinksValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
